name: Build Device Spec Extractor APK

on:
  push:
    branches: [master, main]
    paths:
      - 'app-device-spec/**'
      - '.github/workflows/device-spec-build.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: device-spec-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Assemble debug APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Validate Gradle settings
        working-directory: app-device-spec
        run: gradle --no-daemon help

      - name: Resolve dependencies
        working-directory: app-device-spec
        run: gradle --no-daemon :app:dependencies --configuration debugCompileClasspath

      - name: Compile Kotlin
        working-directory: app-device-spec
        run: gradle --no-daemon --stacktrace :app:compileDebugKotlin

      - name: Assemble debug APK
        working-directory: app-device-spec
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Collect APK
        working-directory: app-device-spec
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/Device-Spec-Extractor.apk
          ls -lh dist/
          ls -lh app/build/outputs/apk/debug/

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Device-Spec-Extractor-debug-apk
          path: app-device-spec/dist/Device-Spec-Extractor.apk
          if-no-files-found: error
