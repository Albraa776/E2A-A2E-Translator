<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using only platform attributes (no Material dependency). -->
    <!-- Compose Material3 handles in-app theming via DeviceSpecExtractorTheme. -->
    <style name="Theme.DeviceSpecExtractor" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>
</resources>
